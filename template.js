// Template Literals - ES6+
// Backtick characters - `` - above tab (left from one)
// Interpolation ${} - insert expression(value)

const name = 'sumit';

const age = 25;

const sentence = "Hey it's " + name + ' and he is ' + age + ' year old';

const value = `Hey it's ${name} and he is ${age} year old`;

console.log(value);

console.log(sentence);

